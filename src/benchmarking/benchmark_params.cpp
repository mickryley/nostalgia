
#include "benchmarking/benchmark_params.h"

namespace nostalgia {
    /*
    void BenchmarkParams::mergeFromCLI(const std::unordered_map<std::string, std::string>& cliArgs){

    }
    */
    /*
    
    BenchmarkParams p;
        p.set("blockSize", 128);                  // Stored in Holder<int>
        p.set("name", std::string("TestAlloc"));  // Stored in Holder<std::string>

        int block = p.get<int>("blockSize");      // type-safe
        std::string name = p.get<std::string>("name");

    
    
    */
}